<div>
  <p>It looks like your cart is currently empty!</p>
  <a href="/">Take me back to the arboretum</a>
</div>

<style>
  div {
    border-radius: 8px;
    background-color: white;
    border: 1px solid var(--color-text-ink-200);
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 1.5rem 2rem;
    gap: 1rem;
    color: var(--color-text-ink-400);
  }

  p {
    margin: 0;
  }

  a {
    text-decoration: none;
    background-color: var(--color-text-ink-500);
    border: 1px solid var(--color-text-ink-200);
    color: var(--color-text-ink-100);
    min-width: 15rem;
    width: 100%;
    max-width: 30rem;
    text-align: center;
    padding: 0.5rem 1rem;
  }

  a:hover {
    text-decoration: underline;
    color: var(--color-text-white);
  }

  a:active {
    transform: scale(0.99);
  }
</style>